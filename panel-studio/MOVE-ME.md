# このフォルダは一時退避です

`panel-studio` は**独立したリポジトリ**で開発する方針です。
ここに置いてあるのは、GitHub 側にリポジトリがまだ無い間にセッションが終了しても
コードが失われないようにするための、一時的なコピーです。

`67Fujiwara/panel-studio`（private）が作成されたら、この `panel-studio/` フォルダは
このリポジトリから削除し、以降はそちらで開発します。

内容については `README.md` と `docs/01-spec-review.md` を参照してください。
